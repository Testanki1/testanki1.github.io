name = "testanki1"
pages_build_output_dir = "."

# 1. 绑定你的 KV 数据库
[[kv_namespaces]]
binding = "SFX_NAMES"
id = "SFX_KV"

# 2. 声明耐用对象类与绑定
[[durable_objects.bindings]]
name = "SFX_HUB"
class_name = "SfxHub"

# 3. 告诉系统在云端自动创建这个耐用对象（核心所在）
[[migrations]]
tag = "v1"
new_classes = ["SfxHub"]
